CREATE TABLE VENDAS (
   ID INT PRIMARY KEY AUTO_INCREMENT,
   VALOR_TOTAL NUMERIC(5,2),
   USUARIO_ID INT REFERENCES USUARIOS(ID)
);

SELECT *FROM VENDAS

INSERT INTO VENDAS(VALOR_TOTAL, USUARIO_ID)
VALUES
(250,1);

SELECT * FROM VENDAS 
   INNER JOIN USUARIOS 
        ON VENDAS.USUARIO_ID = USUARIOS.ID;

SELECT * FROM VENDAS 
   INNER JOIN USUARIOS 
        ON VENDAS.USUARIO_ID = USUARIOS.ID
   INNER JOIN PROFESSORES 
         ON PROFESSORES.ID = USUARIOS.ID
WHERE USUARIOS.ID = 1;

SELECT * FROM VENDAS  V
   INNER JOIN USUARIOS U 
        ON V.USUARIO_ID = U.ID
   INNER JOIN PROFESSORES P
         ON P.ID = U.ID
WHERE U.ID = 1

SELECT * FROM USUARIOS U
INNER JOIN VENDAS V
ON V.USUARIO_ID = U.ID;

SELECT * FROM USUARIOS U
LEFT JOIN VENDAS V
ON V.USUARIO_ID = U.ID;

SELECT * FROM VENDAS V
RIGHT JOIN USUARIOS U
ON V.USUARIO_ID = U.ID;