CREATE TABLE ENDERECOS (
   ID INT PRIMARY KEY AUTO_INCREMENT,
   RUA VARCHAR(255),
   NUMERO INT,
   CEP VARCHAR(255),
   CIDADE VARCHAR(255),
   ESTADO VARCHAR(2)
);

CREATE TABLE PRODUTOS (
ID INT PRIMARY KEY AUTO_INCREMENT,
NOME VARCHAR (255),
DESCRICAO VARCHAR (255),
PRECO NUMERIC(5,2)
);

CREATE TABLE VENDA_PRODUTO(
ID INT PRIMARY KEY AUTO_INCREMENT,
VENDA_ID INT REFERENCES VENDAS(ID),
PRODUTO_ID INT REFERENCES PRODUTOS(ID)
);

ALTER TABLE USUARIOS
ADD ENDERECO_ID INT REFERENCES ENDERECOS(ID);

INSERT INTO ENDERECOS 
(RUA, NUMERO, CEP, CIDADE, ESTADO)
   VALUES 
('Quadra QS 8 Conjunto 210A', 163, '71973-190', 'Brasília', 'DF');

SELECT * FROM USUARIOS

UPDATE  USUARIOS
   SET ENDERECO_ID =4
      WHERE ID = 8;